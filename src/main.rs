/* ROAD TO AGDA-TUI
 *
 *  1. Agda-tui needs to proxy agda-language-server (or directly spawn agda --interaction-json).
 *  2.parse the JSON responses from Agda to populate the TUI panes
 *  (e.g., showing the context, current goals, and syntax-highlighted code).
 *  3. Use Ratatui to draw the interactive interface
 *
 */

fn main() {
    println!("Hello, world!");
}
